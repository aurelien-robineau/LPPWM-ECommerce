<a href="{{ path('product_show', {id: item.getProduct.id}) }}">
	<div class="card basket-item-card">
		<div class="card-body">
			<div class="row">
				<div class="col col-2 image-container">
					<img class="image" src="{{ item.getProduct.image }}" alt="{{ item.getProduct.title }}"/>
				</div>
				<div class="col col-6 col-sm-8">
					<h5 class="h6 title">{{ item.getProduct.title }}</h5>
				</div>
				<div class="col col-4 col-sm-2">
					<div class="price">{{ item.getProduct.price }} €</div>
				</div>
			</div>
		</div>
	</div>
</a>